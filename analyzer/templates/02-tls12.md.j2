{# TLS 1.2 report -#}
# TLS 1.2 Handshake — Mutual Authentication with ECDSA P-256

## 1. Capture summary

- **pcap**: `output/pcap/tls12.pcap`
- **Packets**: {{ tls.packet_count }}
- **Duration**: ~{{ "%.1f"|format(tls.duration_ms) }} ms
- **Client → Server**: {{ tls.client_addr }} → {{ tls.server_addr }}:4443
- **Decryption**: `output/pki/sslkeys-tls12.log` (client SSLKEYLOG)

## 2. Handshake at a glance

{{ tls.mermaid }}

## 3. Negotiated parameters

| Parameter | Value |
|-----------|-------|
| Protocol | TLS 1.2 |
| Cipher | {{ tls.negotiated_cipher_friendly }} |
| Key exchange | ECDHE on P-256 |
| Signature algorithm | {{ tls.sigalg_friendly }} |
| Client auth | mTLS (client Certificate + CertificateVerify) |

## 4. Per-message walk-through

{% for step in tls.handshake_steps %}
### {{ loop.index }}. {{ step.msg_type }} (packet #{{ step.packet_num }})

- Time: {{ step.time }}
{% if step.cipher %}- Cipher: {{ step.cipher }}
{% endif %}
{% if step.sigalg %}- Signature algorithm: {{ step.sigalg }}
{% endif %}
{% if step.extensions %}- Extensions: {{ step.extensions }}
{% endif %}
- Length: {{ step.length }} bytes

{% else %}
*(No handshake messages decoded — ensure make tls12 completed and pcap exists.)*
{% endfor %}

## 5. Application data

- Application data bytes (approx): {{ tls.app_data_bytes }}
- {{ tls.http_request }}
- {{ tls.http_response_hint }}
- nginx sets `X-TLS-Version`, `X-TLS-Cipher`, and `X-Client-CN` response headers when mTLS succeeds.

## 6. Comparison to TLS 1.3

See [03-tls13-handshake.md](03-tls13-handshake.md). TLS 1.3 encrypts most handshake messages after ServerHello; TLS 1.2 exposes Certificate and ServerKeyExchange in cleartext.
